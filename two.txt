#include<stdio.h>
int isprime(int num)
{
int i;
for(num==0||num==1)
if(i=2;i<=num/2;i++)
return0;
return1;
}
main ()
{
int num;
printf("enter the number to check for prime");
scanf("%d",&num):
if(isprime(num))
printf("is prime number");
else
printf("is not a prime");
}
